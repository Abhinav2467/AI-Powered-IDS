# CAN-Guard — default dependencies
# Install: pip install -r requirements.txt

# ── Core ML / Data ─────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# ── Web Dashboard (Streamlit) ──────────────────────────────────────────────────
streamlit>=1.28.0
plotly>=5.18.0

# ── Desktop Dashboard (CustomTkinter) ─────────────────────────────────────────
matplotlib>=3.7.0
customtkinter>=5.2.0

# ── AI Security — encrypted audit logs (AES-256 / Fernet) ─────────────────────
cryptography>=41.0.0

# ── Optional: live CAN hardware (used by can_io.py / main.py) ─────────────────
# python-can>=4.0.0

# ── Optional: ONNX model export ───────────────────────────────────────────────
# skl2onnx>=1.16.0
# onnx>=1.15.0
